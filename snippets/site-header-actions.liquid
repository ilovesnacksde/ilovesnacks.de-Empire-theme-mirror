<ul class="site-header-actions" data-header-actions>



<li class="site-header-actions__language">
  {% form 'localization' %}
    <input type="hidden" name="locale_code" value="{% if request.locale.iso_code == 'ru' %}de{% else %}ru{% endif %}">
    <input type="hidden" name="return_to" value="{{ request.path }}">

    <button type="submit" class="lang-pill" aria-label="Switch language">
      {% if request.locale.iso_code == 'ru' %}
        DE
      {% else %}
        RU
      {% endif %}
    </button>
  {% endform %}
</li>






  {% if shop.customer_accounts_enabled %}
    {% unless customer %}
      <li class="site-header-actions__account-link">
        <a
          class="site-header_account-link-anchor"
          href="{{ routes.account_login_url }}"
        >
          <span class="site-header__account-icon">
            {% 
                render 'icon-library', 
                id: 'icon-account', 
            %}
          </span>
          
          <span class="site-header_account-link-text">
            {{ 'header.account.login' | t }}
          </span>
        </a>
      </li>
    {% else %}
      <li class="site-header-actions__account-link">
        <span class="site-header__account-icon">
          {% render 'icon-library', id: 'icon-account' %}
        </span>
        <span class="site-header__account-links">
          <a
            class="site-header__account-link--account"
            href="{{ routes.account_url }}"
          >
            {{ 'header.account.title' | t }}
          </a>
          
          <a
            class="site-header__account-link--logout"
            href="{{ routes.account_logout_url }}"
            data-no-instant
          >
            {{ 'header.account.logout' | t }}
          </a>
        </span>
      </li>
    {% endunless %}
  {% endif %}
</ul>