<!-- LANG SWITCHER RENDERED -->

{% if localization.available_languages.size > 1 %}
  {% form 'localization', class: 'lang-switcher' %}
    {% for language in localization.available_languages %}
      <button
        type="submit"
        name="locale_code"
        value="{{ language.iso_code }}"
        class="lang-btn {% if language.iso_code == localization.language.iso_code %}active{% endif %}"
        aria-label="{{ language.name }}"
      >
        {% if language.iso_code == 'ru' %}🇷🇺{% endif %}
        {% if language.iso_code == 'de' %}🇩🇪{% endif %}
        {% if language.iso_code == 'lv' %}🇱🇻{% endif %}
        {% if language.iso_code == 'lt' %}🇱🇹{% endif %}
        {% if language.iso_code == 'et' %}🇪🇪{% endif %}
      </button>
    {% endfor %}
  {% endform %}
{% endif %}
